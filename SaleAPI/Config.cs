namespace SaleAPI
{
    public static class Config
    {
        public const string Name = "Admin";
        public const string Password = "Admin"; // it must be in another secret container, not here. For test only!
    }
}
